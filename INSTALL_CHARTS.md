# Instalação das dependências para gráficos
# Execute este comando no terminal na pasta raiz do projeto:

npm install chart.js react-chartjs-2

# Ou se estiver usando yarn:
# yarn add chart.js react-chartjs-2